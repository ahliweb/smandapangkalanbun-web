---
import Layout from "~/layouts/Layout.astro";
import { getHomePermalink } from "~/utils/permalinks";
import { getLocale, t } from "~/utils/i18n";

const locale = getLocale(Astro.request);
const title = t("error_page.404.title", locale);
---

<Layout metadata={{ title }}>
  <section class="flex items-center h-full p-16">
    <div
      class="container flex flex-col items-center justify-center px-5 mx-auto my-8"
    >
      <div class="max-w-md text-center">
        <h2 class="mb-8 font-bold text-9xl">
          <span class="sr-only">Error</span>
          <span class="text-primary">404</span>
        </h2>
        <p class="text-3xl font-semibold md:text-3xl">
          {t("error_page.404.message", locale)}
        </p>
        <p class="mt-4 mb-8 text-lg text-muted dark:text-slate-400">
          {t("error_page.404.description", locale)}
        </p>
        <a rel="noopener noreferrer" href={getHomePermalink()} class="btn ml-4"
          >{t("error_page.404.back_to_home", locale)}</a
        >
      </div>
    </div>
  </section>
</Layout>
